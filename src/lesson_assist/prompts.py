"""고정 프롬프트 템플릿.

프롬프트는 코드로 고정하여 항상 동일한 포맷으로 출력한다.
"""

PART_SUMMARY_SYSTEM = """\
너는 대학 강의 녹취 전사본을 구조화하여 요약하는 전문가이다.
한국어로 작성하며, 학부생이 복습에 바로 사용할 수 있는 품질을 목표로 한다.
교수의 구어체 표현은 정제된 서술체로 바꾸되, 핵심 의미와 뉘앙스는 보존한다."""

PART_SUMMARY_USER = """\
다음은 강의 전사본의 **Part {part_index}** ({time_range}) 이다.

<transcript>
{text}
</transcript>

아래 구조로 요약하라:

## 이 파트의 주제
(1줄)

## 목차
(섹션 5~10개, 번호 매기기)

## 핵심 개념/정의
- 등장한 용어·개념을 정확히 정의

## 정리/규칙/조건
- 수학적 정리, 알고리즘 규칙, 조건 등이 있으면 엄밀히 기술

## 예제/직관
- 교수가 든 예시나 직관적 설명

## 교수 강조 포인트
- "시험에 나온다", "중요", "기억해라", "자주 출제" 등의 신호가 있는 내용

## 헷갈리는 포인트
- 오해하기 쉬운 부분, 교수가 "주의" "구별" 등으로 짚은 부분

## 과제/읽을거리/다음 예고
- 언급된 것만 기록 (없으면 "없음")"""

# RAG 컨텍스트가 있을 때 파트 요약 프롬프트에 주입하는 접두사
PART_SUMMARY_RAG_PREFIX = """\
아래는 이전 강의에서 다룬 관련 내용이다. 이번 강의 요약 시 참고하라.
교수가 "지난 시간에", "이전에 했던" 등으로 언급하는 내용이 아래에 있을 수 있다.

<previous_context>
{rag_context}
</previous_context>

---

"""

INTEGRATED_SUMMARY_SYSTEM = """\
너는 대학 강의 파트별 요약을 하나의 통합 학습 문서로 재구성하는 전문가이다.
중복을 제거하고, 선행개념 → 후행개념 순서로 흐름을 재정렬한다.
한국어로 작성하며, 최종 결과는 그 자체로 해당 강의의 학습 자료가 되어야 한다."""

INTEGRATED_SUMMARY_USER = """\
다음은 한 강의를 파트별로 요약한 결과이다.

과목: {course}
날짜: {date}

{part_summaries}

위 파트별 요약을 하나의 **통합 학습 문서**로 재구성하라:

1. **전체 목차**를 새로 구성 (파트 구분 없이, 주제 흐름에 따라)
2. **중복 내용** 제거
3. **선행개념 → 후행개념** 순서로 재배치
4. 정의, 정리, 예제를 적절히 재배치하여 읽기 좋은 학습 문서로 완성
5. 각 섹션에 내용이 충분히 포함되어야 함 (요약의 요약이 아닌, 학습 가능한 수준)"""

ACTIONS_SYSTEM = """\
너는 대학 강의 전사본에서 학생이 수행해야 할 행동 항목(과제, 시험, 일정)을 추출하는 전문가이다.
추출 대상이 아닌 일반 강의 내용은 무시한다.
반드시 JSON 배열로만 응답하라."""

ACTIONS_USER = """\
다음은 대학 강의 전사본이다.

과목: {course}
날짜: {date}

<transcript>
{text}
</transcript>

위 전사본에서 **학생이 수행해야 할 행동 항목**을 추출하라.

추출 대상:
- 과제/레포트/숙제 (제출물, 마감일)
- 시험/퀴즈 (날짜, 범위)
- 일정 변경 (휴강, 보강, 수업 시간 변경)
- 공지 (이클래스 확인, 자료 업로드 등)

아래 JSON 배열 형식으로만 응답하라 (행동 항목이 없으면 빈 배열 []):

```json
[
  {{
    "type": "과제" | "시험" | "일정" | "공지",
    "content": "구체적 내용",
    "deadline": "마감일/날짜 (언급된 경우, 없으면 null)",
    "priority": "high" | "medium" | "low"
  }}
]
```"""

# 시험 대비 A4 압축 문서 프롬프트
EXAM_SHEET_SYSTEM = """\
너는 대학 시험 직전 5분 복습용 압축 문서를 만드는 전문가이다.
A4 1장 분량으로, 핵심만 남기되 빠뜨리면 안 되는 것은 절대 빠뜨리지 않는다.
한국어로 작성한다."""

EXAM_SHEET_USER = """\
다음은 {course} 과목의 최근 강의 요약 {lecture_count}개를 합친 것이다.

{summaries}

위 내용을 기반으로 **시험 대비 A4 1장 압축 문서**를 작성하라.

아래 구조를 따르라:

## 암기할 것
- 정의, 공식, 조건 (빠뜨리면 안 되는 것들)

## 이해할 것
- 원리, 흐름, 비교 (왜 그런지 설명할 수 있어야 하는 것들)

## 문제풀이 포인트
- 접근법, 유형별 풀이 전략

## 자주 나오는 함정 3개
- 학생들이 실수하기 쉬운 포인트

## 5분 초압축 10줄
- 시험 직전에 훑어볼 수 있는 핵심 10줄"""

EXAM_SHEET_RANGE_USER = """\
다음은 {course} 과목의 시험 범위({range_desc})에 해당하는 강의 요약이다.

{summaries}

위 내용을 기반으로 **시험 대비 A4 1장 압축 문서**를 작성하라.

아래 구조를 따르라:

## 암기할 것
- 정의, 공식, 조건 (빠뜨리면 안 되는 것들)

## 이해할 것
- 원리, 흐름, 비교 (왜 그런지 설명할 수 있어야 하는 것들)

## 문제풀이 포인트
- 접근법, 유형별 풀이 전략

## 자주 나오는 함정 3개
- 학생들이 실수하기 쉬운 포인트

## 5분 초압축 10줄
- 시험 직전에 훑어볼 수 있는 핵심 10줄"""
