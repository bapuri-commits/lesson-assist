# lesson-assist Handoff — 2026-03-17

## 현재 상태
packer.py 크롤링 대상을 eclass 전용으로 변경 완료.

## 작업 내용
- `packer.py`의 school_sync subprocess 호출에 `--site eclass` 옵션 추가
- 기존: `main.py --download` (전체 사이트 크롤링)
- 변경: `main.py --site eclass --download` (eclass만 크롤링)

## 핵심 결정 & 맥락
- NotebookLM 패키지 생성에 필요한 건 eclass 데이터뿐 — portal/department/ndrims 크롤링은 불필요
- school_sync의 `main.py`가 이미 `--site` 옵션을 지원하므로 인자 추가만으로 해결

## 다음 작업
- [ ] VPS 배포: lesson-assist 코드는 볼륨 마운트이므로 `cd /opt/apps/lesson-assist && git pull`만 필요

## 세션 컨텍스트
- **대화 주제**: 크롤링 범위 축소 + StudyHub 감사 (school_sync 중심)
- **마지막 작업**: eclass 전용 크롤링 커밋 + 푸시
