# lesson-assist Handoff — 2026-03-09

## 현재 상태
v2 pack 파이프라인 동작. context_builder 로직을 school_sync로 이동 완료. lesson-assist는 school_sync가 생성한 파일을 읽기만 하는 구조로 전환됨.

## 작업 내용
- `context_builder.py` 삭제 — 로직이 school_sync `context_export.py`로 이동됨
- `packer.py` 수정 — `build_context()` 호출을 `_load_context()` 파일 읽기로 교체
- `config.py` 수정 — `SchoolSyncConfig`에서 `output_dir` + `normalized_dir` 제거, `context_dir` + `context_path`로 단순화
- `config.yaml.example` 수정 — `output_dir` → `context_dir` 키 변경
- `docs/PIPELINE_V2.md` 갱신 — 역할 분리 테이블, Phase 2 설명, 모듈 목록, 설정 예시 전면 갱신
- `docs/PLAN.md` 갱신 — context_builder 참조를 school_sync 이동 안내로 변경

## 핵심 결정 & 맥락
- **역할 확정**: lesson-assist = 패키징 + 포맷팅 + 전달 도구. 데이터 가공은 school_sync에서 전담
- **의존 인터페이스 단순화**: lesson-assist는 school_sync의 내부 정규화 구조(`output/normalized/`)를 몰라도 됨. `output/context/{과목}.md` 파일만 읽으면 됨
- **school_sync 컨텍스트 파일에 YAML frontmatter 포함**: 과목명, 생성시각, 현재 주차/토픽. Phase 4 (note) 구현 시 메타데이터 파싱에 활용

## 다음 작업
- [ ] lesson-assist 리팩토링 범위 확정 (v1 legacy 정리 수준)
- [ ] note 서브커맨드 구현 (Phase 4: NotebookLM → Obsidian)
- [ ] 테스트 복구 (conftest.py 임포트 경로 수정 — legacy 이동 반영)
- [ ] README 갱신 (v2 CLI 구조 반영)
- [ ] The Record `_index.md` 생성
- [ ] E2E 테스트 (실제 다글로 파일 + school_sync 데이터)

## 세션 컨텍스트
- **대화 주제**: school_sync + lesson-assist 프로젝트 비전 확정 및 구조 리팩토링
- **주요 논의 흐름**: 진행상황 보고 → 비전 평가 → context_builder 이동 결정 → 구현 → 코드 검증
- **마지막 작업**: 코드 검증 완료 (잔존 참조 0건, 린트 에러 0건 확인)
- **참고**: 현재 config.yaml은 v1 포맷. v2 CLI 사용 시 config.yaml.example 기반으로 재생성 필요
